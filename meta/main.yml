galaxy_info:
  author: Christian Buggle
  description: Creates ansible_user with passwordless sudo & ssh login, disallows root ssh access. (Boilerplate for new Ansible server)

  license: MIT

  min_ansible_version: 2.6

  # By now tested only on Ubuntu 18.04. Will probably run on any Debian.

  platforms:
    - name: Ubuntu
      versions:
        - utopic
        - xenial
        - bionic

  galaxy_tags:
    - ansible
    - deploy
    - user
    - boilerplate

dependencies: []
